export function getArticles (articles) {
  return {
    type: "GET_ARTICLES",
    articles: articles
  }
}

export function clearArticles (articles) {
  return {
    type: "GET_ARTICLES",
    articles: articles
  }
}